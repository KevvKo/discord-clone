import { render, screen } from '@testing-library/react';
import UserBar from './UserhBar';

// test('renders learn react link', () => {
//   render(<UserBar />);
//   const linkElement = screen.getByText(/ xyz /i);
//   expect(linkElement).toBeInTheDocument();
// });
