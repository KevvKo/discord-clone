  datasource db {
  provider = "mysql"
  url      = env("DB_URL")
}

generator client {
  provider = "prisma-client-js"
}

model user {
  id                Int           @default(autoincrement()) @id
  name          String?       @unique
  email             String?       @unique
  password          String?
  phoneNumber       String?         
  status            Boolean
  active            Boolean
}
