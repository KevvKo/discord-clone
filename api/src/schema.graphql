type Query{
  user: User!
}

type Mutation {
  signup ( email: String!, password: String!, username: String!, status: Boolean! ): AuthPayload
  login ( email: String!, password: String! ): AuthPayload
}

type AuthPayload {
  token: String
  user: User
}

type User {
  id: ID!
  username: String!
  email: String!
  status: Boolean!
}
